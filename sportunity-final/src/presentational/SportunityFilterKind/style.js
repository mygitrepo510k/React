import {
  StyleSheet
} from 'react-native';

import {
  colors
} from '../../theme';

export default StyleSheet.create({
  tabContainer: {
    flexDirection: 'row',
    borderTopColor: colors.lightGrey,
    borderTopWidth: 1
  }
});
